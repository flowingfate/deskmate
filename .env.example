# Local development overrides for runtime process.env (loaded by main.ts in dev).
# Copy to .env.local — gitignored.
# This file is NOT used at build time; all build constants live in source.

# Eval harness auth token (required when running --eval-mode).
# EVAL_AUTH_TOKEN=
